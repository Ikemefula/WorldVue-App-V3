//src/utils/TimeManager.js
//Handles all time related background processes and time related data points
//syncs time, ensures time is correct, manages process intervals
import TimeHelper from './timeHelper';
import RandomHelper from './randomHelper';

class TimeManager {
   constructor(){
      this.timeInitialized = false;
      this.uptime = 0;
   }

   initialze(){

   }

   isTimeValid(){
      //check if the year
   }

   syncTime(){

   }
}

const timeManager = new TimeManager();
export default timeManager